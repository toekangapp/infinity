# ======================================
# JagoHRIS - Laravel Absensi Backend
# Environment Configuration Template
# ======================================

# Application Settings
APP_NAME=JagoHRIS
APP_ENV=production
APP_KEY=
APP_DEBUG=false
APP_TIMEZONE=Asia/Jakarta
APP_URL=https://your-domain.com

# Auto-fill Login (Set to empty values to disable in production)
DEFAULT_LOGIN_EMAIL=admin@admin.com
DEFAULT_LOGIN_PASSWORD=password

# Localization
APP_LOCALE=id
APP_FALLBACK_LOCALE=en
APP_FAKER_LOCALE=id_ID

# Maintenance
APP_MAINTENANCE_DRIVER=file
APP_MAINTENANCE_STORE=database

# Security
BCRYPT_ROUNDS=12

# Logging
LOG_CHANNEL=stack
LOG_STACK=single
LOG_DEPRECATIONS_CHANNEL=null
LOG_LEVEL=error

# Database Configuration
DB_CONNECTION=mysql
DB_HOST=127.0.0.1
DB_PORT=3306
DB_DATABASE=jagohris_db
DB_USERNAME=root
DB_PASSWORD=your_password

# Session Configuration
SESSION_DRIVER=database
SESSION_LIFETIME=120
SESSION_ENCRYPT=false
SESSION_PATH=/
SESSION_DOMAIN=null

# Queue & Broadcasting
BROADCAST_CONNECTION=log
QUEUE_CONNECTION=database

# Cache Configuration
CACHE_STORE=database
CACHE_PREFIX=jagohris

# Filesystem
FILESYSTEM_DISK=local

# Redis (Optional - for advanced caching)
REDIS_CLIENT=phpredis
REDIS_HOST=127.0.0.1
REDIS_PASSWORD=null
REDIS_PORT=6379

# Memcached (Optional)
MEMCACHED_HOST=127.0.0.1

# ======================================
# EMAIL CONFIGURATION
# ======================================

# Option 1: Brevo SMTP (Recommended)
MAIL_MAILER=smtp
MAIL_HOST=smtp-relay.brevo.com
MAIL_PORT=587
MAIL_USERNAME=your_brevo_email
MAIL_PASSWORD=your_brevo_password
MAIL_ENCRYPTION=tls
MAIL_FROM_ADDRESS=noreply@your-domain.com
MAIL_FROM_NAME="JagoHRIS"

# Option 2: Resend (Alternative)
# MAIL_MAILER=resend
# RESEND_API_KEY=re_xxxxxxxxxxxxxxxxxxxxxxxx

# Option 3: Gmail SMTP (Not recommended for production)
# MAIL_MAILER=smtp
# MAIL_HOST=smtp.gmail.com
# MAIL_PORT=587
# MAIL_USERNAME=your-email@gmail.com
# MAIL_PASSWORD=your-app-password
# MAIL_ENCRYPTION=tls

# Option 4: Local Development (Log only)
# MAIL_MAILER=log

# ======================================
# FIREBASE CONFIGURATION (FCM)
# ======================================
FIREBASE_PROJECT_ID=your-firebase-project-id
FIREBASE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nYOUR_PRIVATE_KEY_HERE\n-----END PRIVATE KEY-----\n"
FIREBASE_CLIENT_EMAIL=firebase-adminsdk-xxxxx@your-project.iam.gserviceaccount.com
FCM_SERVER_KEY=your_fcm_server_key

# ======================================
# AWS S3 CONFIGURATION (Optional)
# ======================================
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=us-east-1
AWS_BUCKET=
AWS_USE_PATH_STYLE_ENDPOINT=false

# ======================================
# THIRD-PARTY SERVICES
# ======================================

# Google Maps API (for location services)
GOOGLE_MAPS_API_KEY=your_google_maps_api_key

# Pusher (for real-time features - Optional)
PUSHER_APP_ID=
PUSHER_APP_KEY=
PUSHER_APP_SECRET=
PUSHER_HOST=
PUSHER_PORT=443
PUSHER_SCHEME=https
PUSHER_APP_CLUSTER=mt1

# ======================================
# VITE FRONTEND ASSETS
# ======================================
VITE_APP_NAME="${APP_NAME}"
VITE_PUSHER_APP_KEY="${PUSHER_APP_KEY}"
VITE_PUSHER_HOST="${PUSHER_HOST}"
VITE_PUSHER_PORT="${PUSHER_PORT}"
VITE_PUSHER_SCHEME="${PUSHER_SCHEME}"
VITE_PUSHER_APP_CLUSTER="${PUSHER_APP_CLUSTER}"

# ======================================
# PRODUCTION OPTIMIZATIONS
# ======================================

# Performance Settings
SCOUT_DRIVER=null
SCOUT_PREFIX="${APP_NAME}_"

# Session Security (Production)
SESSION_SECURE_COOKIE=false # Set to true if using HTTPS
SESSION_SAME_SITE=lax

# API Rate Limiting
SANCTUM_STATEFUL_DOMAINS=your-domain.com,api.your-domain.com
SANCTUM_GUARD=web

# ======================================
# BACKUP CONFIGURATION (Optional)
# ======================================
BACKUP_DISK=s3
BACKUP_NOTIFICATION_MAIL_TO=admin@your-domain.com

# ======================================
# MONITORING & ANALYTICS (Optional)
# ======================================

# Sentry Error Tracking
SENTRY_LARAVEL_DSN=
SENTRY_TRACES_SAMPLE_RATE=1.0

# New Relic APM
NEW_RELIC_APP_NAME="${APP_NAME}"
NEW_RELIC_LICENSE_KEY=

# ======================================
# CUSTOM APPLICATION SETTINGS
# ======================================

# Company Default Settings
DEFAULT_COMPANY_NAME="PT Technology Indonesia"
DEFAULT_COMPANY_EMAIL="info@company.com"
DEFAULT_COMPANY_ADDRESS="Jakarta, Indonesia"
DEFAULT_OFFICE_LATITUDE=-6.208763
DEFAULT_OFFICE_LONGITUDE=106.845599
DEFAULT_ATTENDANCE_RADIUS=0.5
DEFAULT_WORK_START_TIME=08:00:00
DEFAULT_WORK_END_TIME=17:00:00
DEFAULT_ATTENDANCE_TYPE=both

# File Upload Limits
MAX_UPLOAD_SIZE=2048
ALLOWED_IMAGE_TYPES=jpg,jpeg,png
ALLOWED_DOCUMENT_TYPES=pdf,doc,docx

# QR Code Settings
QR_CODE_EXPIRY_HOURS=24
QR_CODE_REFRESH_MINUTES=60

# Leave Management
DEFAULT_ANNUAL_LEAVE_DAYS=12
DEFAULT_SICK_LEAVE_DAYS=12

# Timezone Settings
COMPANY_TIMEZONE=Asia/Jakarta
REPORT_TIMEZONE=Asia/Jakarta
